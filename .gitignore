.claude/
venv/
__pycache__/
.env

matmul.db        # SQLite database - instance-specific runtime data
matmul.db-wal    # SQLite WAL files (side effects of WAL mode)
matmul.db-shm    # SQLite shared memory file (side effects of WAL mode)
.node_id         # Unique node identifier - should differ per instance